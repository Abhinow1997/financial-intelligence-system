# Data Lake (local)

`raw/`, `processed/`, `features/` are **gitignored** (see root `.gitignore`).
Only tiny, non-sensitive **samples** live in `samples/` and are committed.

Never commit real financial data, credentials, or PII.
